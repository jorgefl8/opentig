name: Windows release draft
on:
  workflow_call:
    inputs:
      tag:
        description: 'Prepared stable tag'
        required: true
        type: string
      commit:
        description: 'Exact commit frozen by Prepare release'
        required: false
        type: string
  push:
    tags: ['v*']
  workflow_dispatch:
    inputs:
      tag:
        description: 'Existing stable tag, for example v0.1.0'
        required: true
        type: string
permissions:
  contents: read
concurrency:
  group: windows-release-${{ inputs.tag || github.ref_name }}
  cancel-in-progress: false
jobs:
  validate:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    outputs:
      tag: ${{ steps.release.outputs.tag }}
      commit: ${{ steps.release.outputs.commit }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ inputs.commit || inputs.tag || github.ref_name }}
          fetch-depth: 0
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - id: release
        run: node scripts/validate-release-tag.mjs
        env:
          OPENTIG_RELEASE_TAG: ${{ inputs.tag || github.ref_name }}
          OPENTIG_RELEASE_COMMIT: ${{ inputs.commit }}
      - run: npm ci
      - run: npm run check
  build:
    needs: validate
    runs-on: windows-latest
    environment: release
    env:
      RELEASE_SIGNING: ${{ vars.WINDOWS_RELEASE_SIGNING || 'unsigned' }}
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.validate.outputs.commit }}
          persist-credentials: false
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Test on Windows before packaging the installer
        run: npm test
      - name: Validate signing mode
        run: |
          if ($env:RELEASE_SIGNING -cnotin @('unsigned', 'signed')) { throw 'WINDOWS_RELEASE_SIGNING must be unsigned or signed.' }
      - name: Build unsigned installer and update metadata
        if: env.RELEASE_SIGNING == 'unsigned'
        run: npm run make -- --release
      - name: Build signed installer and update metadata
        if: env.RELEASE_SIGNING == 'signed'
        run: npm run make -- --release --signed
        env:
          CSC_LINK: ${{ secrets.WINDOWS_CSC_LINK }}
          CSC_KEY_PASSWORD: ${{ secrets.WINDOWS_CSC_KEY_PASSWORD }}
          OPENTIG_PUBLISHER_NAME: ${{ vars.WINDOWS_PUBLISHER_NAME }}
      - run: npm run verify:packaged-desktop
      - run: npm run verify:packaged-server
      - run: ./scripts/verify-windows-install.ps1
      - run: npm run verify:packaged-utility
      - run: npm run verify:packaged-trash
      - run: npm run verify:release
        if: env.RELEASE_SIGNING == 'unsigned'
      - run: npm run verify:release -- --signed
        if: env.RELEASE_SIGNING == 'signed'
        env:
          OPENTIG_PUBLISHER_NAME: ${{ vars.WINDOWS_PUBLISHER_NAME }}
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-release
          path: |
            out/make/production/win32-x64/*Setup.exe
            out/make/production/win32-x64/*Setup.exe.blockmap
            out/make/production/win32-x64/latest.yml
          if-no-files-found: error
          retention-days: 30
          compression-level: 0
  draft:
    needs: [validate, build]
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    concurrency:
      group: opentig-stable-release-draft
      cancel-in-progress: false
      queue: max
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: windows-release
          path: release-assets
      - name: Create draft for review; never replace a published release
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          RELEASE_TAG: ${{ needs.validate.outputs.tag }}
          RELEASE_COMMIT: ${{ needs.validate.outputs.commit }}
          GH_REPO: ${{ github.repository }}
        run: |
          set -euo pipefail
          actual_commit="$(gh api "repos/$GH_REPO/commits/$RELEASE_TAG" --jq .sha)"
          test "$actual_commit" = "$RELEASE_COMMIT"
          if gh release view "$RELEASE_TAG" --json isDraft,body,targetCommitish > release.json 2>/dev/null; then
            test "$(jq -r .isDraft release.json)" = true || { echo 'Refusing to replace a published release.'; exit 1; }
            if jq -e '.body | contains("<!-- opentig:release-notes:start -->")' release.json >/dev/null; then
              test "$(jq -r .targetCommitish release.json)" = "$RELEASE_COMMIT" || {
                echo 'Run Next release draft on main to reconcile notes to this tag, then retry this workflow.'
                exit 1
              }
            fi
          else
            gh release create "$RELEASE_TAG" --verify-tag --draft --title "OpenTig $RELEASE_TAG" --generate-notes
          fi
          gh release upload "$RELEASE_TAG" release-assets/* --clobber
          echo "## $RELEASE_TAG is ready to publish" >> "$GITHUB_STEP_SUMMARY"
          echo "Installer and update metadata validated and attached. Review the draft, then click **Publish release**." >> "$GITHUB_STEP_SUMMARY"
          gh release view "$RELEASE_TAG" --json url --jq .url >> "$GITHUB_STEP_SUMMARY"
